# Changelog

## 0.1.0

- Initial developer release surface for the `ml-runtime` CLI and Rust runtime.
- Added model catalog, acquisition, local/remote execution, streaming,
  observability, capability reporting, and `ml-runtime doctor`.
- Protocol `/v1` remains the compatibility surface.
- Core ML is macOS-only; CPU execution is the portable baseline.
